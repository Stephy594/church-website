import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { FormsModule } from '@angular/forms';import { Firestore,addDoc,collection,collectionData,deleteDoc,doc,orderBy,query,serverTimestamp } from '@angular/fire/firestore';import { Observable } from 'rxjs';interface Announcement{id:string;title:string;message:string;published:boolean;createdAt?:any}@Component({selector:'app-admin-announcements',standalone:true,imports:[CommonModule,FormsModule],templateUrl:'./admin-announcements.html',styleUrl:'./admin-announcements.css'})export class AdminAnnouncements implements OnInit{items$!:Observable<Announcement[]>;form={title:'',message:'',published:true};busy=false;constructor(private fs:Firestore){}ngOnInit(){this.items$=collectionData(query(collection(this.fs,'announcements'),orderBy('createdAt','desc')),{idField:'id'}) as Observable<Announcement[]>}async add(){if(!this.form.title.trim()||!this.form.message.trim())return;this.busy=true;try{await addDoc(collection(this.fs,'announcements'),{...this.form,createdAt:serverTimestamp()});this.form={title:'',message:'',published:true}}finally{this.busy=false}}async remove(x:Announcement){if(confirm('Delete this announcement?'))await deleteDoc(doc(this.fs,'announcements',x.id))}}