import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { Firestore,collection,collectionData,deleteDoc,doc,orderBy,query } from '@angular/fire/firestore';import { Observable } from 'rxjs';interface Subscriber{id:string;email:string;name?:string;createdAt?:any}@Component({selector:'app-admin-subscribers',standalone:true,imports:[CommonModule],templateUrl:'./admin-subscribers.html',styleUrl:'./admin-subscribers.css'})export class AdminSubscribers implements OnInit{items$!:Observable<Subscriber[]>;constructor(private fs:Firestore){}ngOnInit(){this.items$=collectionData(query(collection(this.fs,'subscribers'),orderBy('createdAt','desc')),{idField:'id'}) as Observable<Subscriber[]>}async remove(x:Subscriber){if(confirm(`Remove ${x.email}?`))await deleteDoc(doc(this.fs,'subscribers',x.id))}export(items:Subscriber[]){const csv='Email,Name\n'+items.map(x=>`"${x.email}","${x.name||''}"`).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='church-subscribers.csv';a.click();URL.revokeObjectURL(a.href)}}