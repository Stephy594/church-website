import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { FormsModule } from '@angular/forms';import { Firestore,doc,getDoc,serverTimestamp,setDoc } from '@angular/fire/firestore';@Component({selector:'app-admin-bible-verse',standalone:true,imports:[CommonModule,FormsModule],templateUrl:'./admin-bible-verse.html',styleUrl:'./admin-bible-verse.css'})export class AdminBibleVerse implements OnInit{form={reference:'',verse:'',theme:'',published:true};busy=false;message='';constructor(private fs:Firestore){}async ngOnInit(){const snap=await getDoc(doc(this.fs,'siteContent','bibleVerse'));if(snap.exists())this.form={...this.form,...snap.data()} as any}async save(){if(!this.form.reference.trim()||!this.form.verse.trim())return;this.busy=true;try{await setDoc(doc(this.fs,'siteContent','bibleVerse'),{...this.form,updatedAt:serverTimestamp()},{merge:true});this.message='Bible verse updated successfully.'}finally{this.busy=false}}}