import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { FormsModule } from '@angular/forms';import { Firestore,addDoc,collection,collectionData,deleteDoc,doc,orderBy,query,serverTimestamp,updateDoc } from '@angular/fire/firestore';import { Observable } from 'rxjs';
interface ChurchEvent{id:string;title:string;date:string;time:string;location:string;description:string;published:boolean;createdAt?:any}
@Component({selector:'app-admin-events',standalone:true,imports:[CommonModule,FormsModule],templateUrl:'./admin-events.html',styleUrl:'./admin-events.css'})export class AdminEvents implements OnInit{items$!:Observable<ChurchEvent[]>;editing:ChurchEvent|null=null;form={title:'',date:'',time:'',location:'',description:'',published:true};busy=false;message='';constructor(private fs:Firestore){}ngOnInit(){this.items$=collectionData(query(collection(this.fs,'events'),orderBy('createdAt','desc')),{idField:'id'}) as Observable<ChurchEvent[]>}edit(x:ChurchEvent){this.editing=x;this.form={title:x.title,date:x.date,time:x.time,location:x.location,description:x.description,published:x.published}}reset(){this.editing=null;this.form={title:'',date:'',time:'',location:'',description:'',published:true}}async save(){if(!this.form.title.trim()||!this.form.date)return;this.busy=true;try{const data={...this.form,title:this.form.title.trim(),updatedAt:serverTimestamp()};if(this.editing)await updateDoc(doc(this.fs,'events',this.editing.id),data);else await addDoc(collection(this.fs,'events'),{...data,createdAt:serverTimestamp()});this.message='Event saved successfully.';this.reset()}finally{this.busy=false}}async remove(x:ChurchEvent){if(confirm(`Delete ${x.title}?`))await deleteDoc(doc(this.fs,'events',x.id))}}