import { CommonModule } from '@angular/common';import { Component,OnInit } from '@angular/core';import { FormsModule } from '@angular/forms';import { Firestore,collection,collectionData,deleteDoc,doc,orderBy,query,updateDoc } from '@angular/fire/firestore';import { Observable } from 'rxjs';interface Message{id:string;name:string;email:string;subject?:string;message:string;status?:'new'|'read';createdAt?:any}@Component({selector:'app-admin-contact-messages',standalone:true,imports:[CommonModule,FormsModule],templateUrl:'./admin-contact-messages.html',styleUrl:'./admin-contact-messages.css'})export class AdminContactMessages implements OnInit{items$!:Observable<Message[]>;constructor(private fs:Firestore){}ngOnInit(){this.items$=collectionData(query(collection(this.fs,'contactMessages'),orderBy('createdAt','desc')),{idField:'id'}) as Observable<Message[]>}async read(x:Message){await updateDoc(doc(this.fs,'contactMessages',x.id),{status:'read'})}async remove(x:Message){if(confirm('Delete this message?'))await deleteDoc(doc(this.fs,'contactMessages',x.id))}date(v:any){return v?.seconds?new Date(v.seconds*1000).toLocaleString('en-GB'):'Date unavailable'}}